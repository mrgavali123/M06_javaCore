package org.tnsif.interfaceexecuter;
//Program to demonstrate an functional interface

@FunctionalInterface
public interface ChandrayaanThree {
	void status();

	/*Functional interface contain exactly one abstract method
	 * and in interface all method are abstract */
	
	//void displayResult();
	
}
